import {requireNativeComponent} from 'react-native';
// Props: html: string, allowRemote: bool, cacheDir: string, onHeight: event({nativeEvent:{height}})
export default requireNativeComponent('MessageBodyView');
